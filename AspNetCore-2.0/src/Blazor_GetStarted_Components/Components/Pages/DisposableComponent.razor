@page "/Components/DisposableComponent"

@using System
@implements IDisposable


@functions 
{
    // If a component implements IDisposable, the Dispose method is called when the component is removed from the UI. 
    public void Dispose()
    {
    }
}